type FluidLayer = {
  color: string;
  angle: number;
  horizontalPosition: number;
  angularSpeed: number;
  maxAmplitude: number | string;
  frequency: number;
  horizontalSpeed: number;
  initialHeight: number;
};

export { FluidLayer };
